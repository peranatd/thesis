machine:
  services:
    - docker

dependencies:
  override:
    - pip install awsebcli
    - docker info
    - docker build -t $DOCKER_REPO .

test:
  override:
    - docker run $DOCKER_REPO node ./server/lib/wav.js

deployment:
  hub:
    branch: /.*deploy/
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push $DOCKER_REPO