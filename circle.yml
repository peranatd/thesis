machine:
  node:
    version: 6.3.1

  npm:
    version: 3.10.5

  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t peranat/littlebigplanet .

test:
  override:
    - docker run peranat/littlebigplanet exec npm test
